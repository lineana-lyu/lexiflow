name: One-shot Visualize product fix

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/one-shot-visualize-product-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Apply Visualize product fixes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import json, re

          def read(path): return Path(path).read_text(encoding='utf-8')
          def write(path, text): Path(path).write_text(text, encoding='utf-8')
          def sub_once(text, pattern, replacement, label, flags=re.S):
              out, n = re.subn(pattern, replacement, text, count=1, flags=flags)
              if n != 1:
                  raise SystemExit(f'{label}: expected one replacement, got {n}')
              return out
          def replace_once(text, old, new, label):
              if old not in text:
                  raise SystemExit(f'{label}: target not found')
              return text.replace(old, new, 1)

          # 1) AI assist: user's own scene is an anchor, never an avoidance target.
          p='public/ai-assist-v3.js'; t=read(p)
          replacement='''  function sceneHistoryKey(body){
            return `${historyKey(body)}|${normalizeForSimilarity(body?.userScene||body?.previousScene).slice(0,120)}`;
          }

          function sceneRevisionInstruction(failed=""){
            const prior=clean(failed);
            if(!prior)return "";
            return `上一版建议是：${prior}。请只修正具体度、可视化细节或无文字要求，必须继续保留用户原始联想里的主体、地点、核心动作/关系和关键物体，不得换成另一个故事。`;
          }

          async function visualScene(body={}){
            const userScene=clean(body.userScene||body.previousScene);
            if(!userScene)throw new Error("VISUAL_SCENE_SOURCE_REQUIRED");
            const key=sceneHistoryKey({...body,userScene});
            let failed="",last=null;
            for(let attempt=0;attempt<2;attempt++){
              const request={
                ...body,
                userScene,
                previousSuggestion:attempt===0?clean(body.previousSuggestion):failed,
                revisionInstruction:attempt===0?"":sceneRevisionInstruction(failed),
              };
              delete request.previousScene;
              const payload=await postJson("/api/ai/visual-scene",request);last=payload;
              const raw=clean(payload?.assist?.scene);
              const scene=sceneNeedsCleanup(raw,body.word)?sanitizeScene(raw,body):raw;
              if(sceneIsConcrete(scene)){
                remember(sceneHistory,SCENE_HISTORY_KEY,key,scene);
                return {...payload,assist:{...(payload.assist||{}),scene}};
              }
              failed=scene;
            }
            if(last?.assist){
              const raw=clean(last.assist.scene);
              const scene=sceneNeedsCleanup(raw,body.word)?sanitizeScene(raw,body):raw;
              if(scene){remember(sceneHistory,SCENE_HISTORY_KEY,key,scene);return {...last,assist:{...last.assist,scene}};}
            }
            throw new Error("VISUAL_SCENE_EMPTY");
          }

'''
          t=sub_once(t,r'  function sceneAvoidance\(note,previous,failed=""\)\{.*?\n  function promptAvoidance',replacement+'  function promptAvoidance','ai-assist anchor semantics')
          write(p,t)

          # 2) Input guard: expose one shared draft authority and drop the old "ss"-style residue.
          p='public/visualize-input-guard-v3.js'; t=read(p)
          t=replace_once(t,'  const drafts=new Map();\n','''  const drafts=new Map();

  function getDraft(cardId){
    const key=String(cardId||"");
    return key&&drafts.has(key)?drafts.get(key):null;
  }
  function setDraft(cardId,value){const key=String(cardId||"");if(key)drafts.set(key,String(value||""));}
  function clearDraft(cardId){const key=String(cardId||"");if(key)drafts.delete(key);}
  function isLegacyResidue(value){return /^([a-z])\\1{1,2}$/i.test(String(value||"").trim());}
''','guard draft API')
          t=replace_once(t,'    drafts.set(cardId,String(input.value||""));','    setDraft(cardId,input.value);','guard remember')
          t=replace_once(t,'''    if(!cardId||!drafts.has(cardId))return;
    const value=drafts.get(cardId);
    if(input.value===value)return;
    input.value=value;
    updateActions(input);''','''    if(!cardId)return;
    const value=getDraft(cardId);
    if(value===null)return;
    if(isLegacyResidue(value)){
      clearDraft(cardId);
      if(isLegacyResidue(input.value))input.value="";
      updateActions(input);
      return;
    }
    if(input.value===value)return;
    input.value=value;
    updateActions(input);''','guard restore')
          t=replace_once(t,'  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();','''  window.LexiFlowVisualizeInputGuardV3=Object.freeze({getDraft,setDraft,clearDraft});

  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();''','guard export')
          write(p,t)

          # 3) Visualize UI and background image generation.
          p='public/visualize-stage-v3.js'; t=read(p)
          t=replace_once(t,'  const localDrafts=new Map();','  const localDrafts=new Map();\n  const residueCleanupQueued=new Set();','visual residue state')
          old='''  function isBusy(){return assistBusy||imageBusy||uploadBusy;}

  function draftFor(card){
    if(localDrafts.has(card.id))return localDrafts.get(card.id);
    const value=String(card.visualNote||"");
    localDrafts.set(card.id,value);
    return value;
  }

  function setDraft(cardId,value){localDrafts.set(String(cardId),String(value||""));}
'''
          new='''  function isBusy(){return assistBusy||uploadBusy;}
  function isLegacyDraftResidue(value){return /^([a-z])\\1{1,2}$/i.test(String(value||"").trim());}

  async function clearLegacyPersistedResidue(cardId){
    const key=String(cardId||"");if(!key||residueCleanupQueued.has(key))return;
    residueCleanupQueued.add(key);
    try{await saveCardPatch(key,c=>{if(isLegacyDraftResidue(c.visualNote))c.visualNote="";});}catch{}
  }

  function draftFor(card){
    const guard=window.LexiFlowVisualizeInputGuardV3;
    const guarded=guard?.getDraft?.(card.id);
    if(guarded!==null&&guarded!==undefined){
      if(isLegacyDraftResidue(guarded)){guard?.clearDraft?.(card.id);return "";}
      return String(guarded);
    }
    if(localDrafts.has(card.id))return localDrafts.get(card.id);
    let value=String(card.visualNote||"");
    if(isLegacyDraftResidue(value)){
      value="";
      queueMicrotask(()=>void clearLegacyPersistedResidue(card.id));
    }
    localDrafts.set(card.id,value);
    return value;
  }

  function setDraft(cardId,value){
    const key=String(cardId),next=String(value||"");
    localDrafts.set(key,next);
    window.LexiFlowVisualizeInputGuardV3?.setDraft?.(key,next);
  }
'''
          t=replace_once(t,old,new,'visual draft authority')

          t=sub_once(t,r'\.lexi-v3-visual-loading\{.*?\.lexi-v3-visual-panel\{', '''.lexi-v3-visual-loading{position:absolute;inset:0;overflow:hidden;background:linear-gradient(145deg,rgba(250,251,249,.94),rgba(245,248,246,.88));display:grid;place-items:center;text-align:center;padding:28px;pointer-events:none}.lexi-v3-visual-loading:before{content:"";position:absolute;inset:-35%;background:radial-gradient(circle,rgba(120,140,132,.11),transparent 58%);animation:lexiVisualSweep 2.8s ease-in-out infinite}.lexi-v3-generation-status{position:relative;z-index:1;max-width:320px;padding:18px 22px;border:1px solid rgba(120,140,132,.16);border-radius:18px;background:rgba(255,255,255,.72);box-shadow:0 16px 46px rgba(80,94,88,.07);display:grid;justify-items:center;gap:9px}.lexi-v3-generation-mark{width:40px;height:40px;border-radius:14px;display:grid;place-items:center;background:rgba(120,140,132,.08);font-size:17px;animation:lexiVisualPulse 1.8s ease-in-out infinite}.lexi-v3-generation-status strong{font-size:14px;font-weight:650}.lexi-v3-generation-status>span:last-child{font-size:12px;line-height:1.55;color:var(--muted)}.lexi-v3-generation-dots{display:flex;gap:5px;height:8px;align-items:center}.lexi-v3-generation-dots i{width:5px;height:5px;border-radius:50%;background:var(--muted);opacity:.26;animation:lexiVisualDot 1.2s ease-in-out infinite}.lexi-v3-generation-dots i:nth-child(2){animation-delay:.16s}.lexi-v3-generation-dots i:nth-child(3){animation-delay:.32s}@keyframes lexiVisualSweep{0%,100%{transform:translate3d(-8%,-5%,0);opacity:.65}50%{transform:translate3d(8%,5%,0);opacity:1}}@keyframes lexiVisualPulse{0%,100%{transform:scale(.96);opacity:.78}50%{transform:scale(1.04);opacity:1}}@keyframes lexiVisualDot{0%,100%{transform:translateY(0);opacity:.22}50%{transform:translateY(-3px);opacity:.72}}@media(prefers-reduced-motion:reduce){.lexi-v3-visual-loading:before,.lexi-v3-generation-mark,.lexi-v3-generation-dots i{animation:none}}.lexi-v3-visual-panel{''','visual loading style')

          t=replace_once(t,'    const interactionBusy=assistBusy||generating||uploadBusy;','    const blockingBusy=assistBusy||uploadBusy;\n    const assetBusy=assistBusy||uploadBusy||generating;','visual busy split')
          t=replace_once(t,'${generating?`<div class="lexi-v3-visual-loading"><div><span class="mini-spinner"></span><strong>正在生成联想图</strong><span>当前联想正在保存，完成后会自动更新到这张单词卡。</span></div></div>`:""}','${generating?`<div class="lexi-v3-visual-loading" aria-live="polite"><div class="lexi-v3-generation-status"><span class="lexi-v3-generation-mark">✦</span><strong>联想图正在后台生成</strong><span>你可以继续学习，完成后图片会自动保存到这张单词卡。</span><span class="lexi-v3-generation-dots" aria-hidden="true"><i></i><i></i><i></i></span></div></div>`:""}','visual loading markup')
          t=t.replace('||interactionBusy?"disabled":""','||assetBusy?"disabled":""')
          t=t.replace('${interactionBusy?"disabled":""}>采用这个建议','${assetBusy?"disabled":""}>采用这个建议')
          t=t.replace('${!String(note).trim()||interactionBusy?"disabled":""}>${generating?','${!String(note).trim()||assetBusy?"disabled":""}>${generating?')
          t=t.replace('${interactionBusy?"aria-disabled=\\"true\\" style=\\"pointer-events:none;opacity:.55\\"":""}>上传图片','${assetBusy?"aria-disabled=\\"true\\" style=\\"pointer-events:none;opacity:.55\\"":""}>上传图片')
          old_footer='''<div class="learning-stage-footer single-action"><button class="btn primary" type="button" data-action="finish-visual" ${interactionBusy?"disabled title=\\"当前联想正在保存\\"":""}>${interactionBusy?"正在保存当前联想…":"完成视觉联想 · 明天开始造句"}</button></div>'''
          new_footer='''<div class="learning-stage-footer single-action"><button class="btn primary" type="button" data-action="finish-visual" ${blockingBusy?"disabled title=\\"当前联想正在保存\\"":""}>${blockingBusy?"正在保存当前联想…":generating?"图片后台生成中 · 完成视觉联想":"完成视觉联想 · 明天开始造句"}</button></div>'''
          t=replace_once(t,old_footer,new_footer,'visual footer background continuation')

          insert='''

  async function saveCardAnyStagePatch(cardId,mutate){
    const latest=await loadData(true);
    const card=latest?.cards?.find(item=>String(item.id)===String(cardId));
    if(!card)return null;
    const changed=mutate(card,latest);
    if(changed===false)return card;
    card.updatedAt=new Date().toISOString();
    await persist(latest);
    return data?.cards?.find(item=>String(item.id)===String(cardId))||card;
  }
'''
          marker='''  async function speak(){'''
          t=replace_once(t,marker,insert+'\n'+marker,'visual any-stage save helper')
          t=replace_once(t,'const payload=await authority({word:card.word,meaningZh:card.meaningZh,exampleEn:card.exampleEn,senseIntentEn:card.senseIntentEn||"",previousScene:note});','const payload=await authority({word:card.word,meaningZh:card.meaningZh,exampleEn:card.exampleEn,senseIntentEn:card.senseIntentEn||"",userScene:note,previousSuggestion:String(card.visualSceneSuggestion?.scene||"")});','visual assist anchored request')

          new_generate='''  async function generate(){
    if(isBusy()||imageBusy)return;
    const card=currentCard();if(!card)return;
    const note=String(document.getElementById("visual-note")?.value??draftFor(card)).trim();if(!note)return;
    const requestId=`visual-${Date.now()}-${Math.random().toString(16).slice(2)}`;
    const guard=beginVisualWrite(card.id);if(!guard)return;
    setDraft(card.id,note);imageBusy=true;
    let started=null;
    try{
      started=await saveCardPatch(card.id,c=>{c.visualNote=note;c.imageGeneration={status:"generating",phase:"submitted",message:"联想图正在后台生成。",code:"",requestId,startedAt:new Date().toISOString()};});
    }finally{
      guard.transition?.endStageWrite?.(guard.write);
    }
    if(!started){imageBusy=false;syncFromGateway();render();return;}
    render();
    try{
      const response=await fetch("/api/ai/image",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({cardId:card.id,requestId,word:card.word,meaningZh:card.meaningZh,exampleEn:card.exampleEn,visualNote:note,suggestedScene:"",sourceQuery:card.sourceQuery||card.word,senseIntentEn:card.senseIntentEn||"",avoidVisualEn:Array.isArray(card.avoidVisualEn)?card.avoidVisualEn:[]})});
      let payload={};try{payload=await response.json();}catch{}
      if(!response.ok||!payload?.image?.url)throw Object.assign(new Error(payload?.error||"IMAGE_FAILED"),{payload});
      const preparedAt=new Date().toISOString();
      await saveCardAnyStagePatch(card.id,c=>{
        if(String(c.imageGeneration?.requestId||"")!==requestId)return false;
        const autoConfirm=core.canonicalStage(c)!=="visualize"&&c.visualImagePendingAtAdvance===true;
        c.imageData="";c.imageUrl=payload.image.url;c.generatedVisualScene=String(payload.image.visualNote||note||"").trim();
        c.visualImageConfirmed=autoConfirm?true:false;
        if(autoConfirm)c.visualImageConfirmedAt=preparedAt;
        c.visualImagePendingAtAdvance=false;c.visualImagePreparedAt=preparedAt;c.visualImageSource="generated";
        c.imageGeneration={status:"success",phase:"done",message:autoConfirm?"联想图已在后台生成并保存到单词卡。":"联想图已生成并保存为 Visualize 草稿。",code:"",requestId,finishedAt:preparedAt};
      });
    }catch(err){
      const message=err?.payload?.userError?.message||"这次图片没有生成成功，可以重试或上传自己的图片。";
      try{await saveCardAnyStagePatch(card.id,c=>{if(String(c.imageGeneration?.requestId||"")!==requestId)return false;c.visualImagePendingAtAdvance=false;c.imageGeneration={status:"error",phase:"error",message,code:err?.payload?.code||"IMAGE_GENERATION_FAILED",requestId,finishedAt:new Date().toISOString()};});}catch{}
    }finally{imageBusy=false;syncFromGateway();render();}
  }
'''
          t=sub_once(t,r'  async function generate\(\)\{.*?\n  async function upload\(file\)\{',new_generate+'\n  async function upload(file){','visual background generation function')
          t=replace_once(t,'    const interactionBusy=isBusy();','    const interactionBusy=isBusy()||imageBusy;','visual input action state')
          write(p,t)

          # 4) Visualize action decorator: a pending image is a valid background task, not a blocker.
          p='public/visualize-actions-v3.js'; t=read(p)
          t=replace_once(t,'    const hasImage=Boolean(current.imageData||current.imageUrl),busy=stageBusy();','    const hasImage=Boolean(current.imageData||current.imageUrl),pending=current.imageGeneration?.status==="generating",busy=stageBusy();','actions pending state')
          t=replace_once(t,'    if(hasImage){','    if(hasImage||pending){','actions pending branch')
          t=replace_once(t,'        next.textContent=busy?"正在保存当前联想…":"完成视觉联想 · 明天开始造句";\n        next.title=busy?"当前联想正在保存，完成后再进入下一阶段。":"";','''        next.textContent=busy?"正在保存当前联想…":pending?"图片后台生成中 · 完成视觉联想":"完成视觉联想 · 明天开始造句";
        next.title=busy?"当前联想正在保存，完成后再进入下一阶段。":pending?"可以继续学习；图片完成后会自动保存到这张单词卡。":"";''','actions pending copy')
          write(p,t)

          # 5) Stage transition: allow completion while an image job is pending.
          p='public/stage-transition-v3.js'; t=read(p)
          old='''      const hasImage=Boolean(card.imageData||card.imageUrl); if(!hasImage)return false;
      const prev={...card,learningStage:"visualize"};
      const note=String(document.getElementById("visual-note")?.value||card.visualNote||"").trim();
      card.visualNote=note;
      card.visualSkipped=false;
      card.visualImageConfirmed=true;
      card.visualImageConfirmedAt=now.toISOString();'''
          new='''      const hasImage=Boolean(card.imageData||card.imageUrl);
      const imagePending=card.imageGeneration?.status==="generating";
      if(!hasImage&&!imagePending)return false;
      const prev={...card,learningStage:"visualize"};
      const note=String(document.getElementById("visual-note")?.value||card.visualNote||"").trim();
      card.visualNote=note;
      card.visualSkipped=false;
      card.visualImageConfirmed=hasImage;
      card.visualImagePendingAtAdvance=!hasImage&&imagePending;
      if(hasImage)card.visualImageConfirmedAt=now.toISOString();'''
          t=replace_once(t,old,new,'transition pending image')
          t=replace_once(t,'appendActivity(data,card.id,"visualize",{skipped:false,imageConfirmed:true,nextStage:"apply",commandId});','appendActivity(data,card.id,"visualize",{skipped:false,imageConfirmed:hasImage,imagePending:!hasImage&&imagePending,nextStage:"apply",commandId});','transition pending activity')
          write(p,t)

          # 6) Server owns final image attachment so navigating/reloading cannot lose the result.
          p='server.js'; t=read(p)
          new_assist='''async function visualSceneAssist(body){
            const word=String(body.word||"").trim();
            const meaningZh=String(body.meaningZh||"").trim();
            const exampleEn=String(body.exampleEn||"").trim();
            const senseIntentEn=String(body.senseIntentEn||"").trim();
            const userScene=String(body.userScene||body.previousScene||"").trim();
            const previousSuggestion=String(body.previousSuggestion||"").trim();
            const revisionInstruction=String(body.revisionInstruction||"").trim();
            if(!word||!userScene)throw new Error("INVALID_INPUT");
            const settings=await loadSettings();
            const cacheKey=[settings.codexModel||DEFAULT_CODEX_MODEL,word.toLowerCase(),meaningZh,exampleEn,senseIntentEn,userScene].join("|");
            if(!previousSuggestion&&visualSceneCache.has(cacheKey))return visualSceneCache.get(cacheKey);
            const prompt=`你在帮助英语学习者把“他自己已经想到的联想画面”变得更具体。你的任务不是重新创作另一个故事。\n目标词：${word}\n词义：${meaningZh}\n例句：${exampleEn}\n准确语义：${senseIntentEn}\n用户原始联想（最高优先级，必须保留）：${userScene}\n${previousSuggestion?`上一版 AI 建议：${previousSuggestion}`:""}\n${revisionInstruction?`修正要求：${revisionInstruction}`:""}\n要求：\n1. 必须保留用户原始联想里已经出现的主体/人物类型、地点、核心动作或关系、关键物体。只能在这些元素上补充可见细节，不能换人物、换地点、换故事。\n2. 用户写得抽象时，把抽象属性转成同一场景里可看见的动作、队形、表情、姿态或物体关系；例如用户写的是某类人物，就继续围绕这类人物具体化，不能替换成另一类人物。\n3. 场景必须严格对应当前词义，并且一眼能画出来；不要添加与用户原始联想无关的新主角或新情节。\n4. scene 中不得出现目标英文单词、中文释义、标签、招牌、logo、屏幕文字、字母或数字。\n5. 如果涉及包装、书本、屏幕、菜单或路牌，只保留无品牌、无可读文字的版本。\n6. 优先一个清晰动作和一个视觉焦点，保持自然生活感。\n7. cue 只概括用户自己的记忆钩子；practiceQuestion 可以围绕同一真实场景追问，不要另起场景。\n只输出 JSON：{"scene":"在用户原始联想基础上具体化的一句中文画面","cue":"6~16字记忆钩子","practiceQuestion":"一句简短中文问题"}`;
            const result=await runCodexFastText(prompt,{timeoutMs:30000,reasoningEffortOverride:"low"});
            const parsed=extractJson(result.stdout);
            const assist={scene:String(parsed.scene||"").trim(),cue:String(parsed.cue||"").trim(),practiceQuestion:String(parsed.practiceQuestion||"").trim()};
            if(!assist.scene)throw new Error("EMPTY_VISUAL_SCENE");
            if(!previousSuggestion)putSmallCache(visualSceneCache,cacheKey,assist);
            return assist;
          }'''
          t=sub_once(t,r'async function visualSceneAssist\(body\)\{.*?\n\}\n\nasync function practicePromptAssist',new_assist+'\n\nasync function practicePromptAssist','server anchored assist')

          helper='''
          async function attachVisualGenerationToCard(body,image,errorInfo=null){
            const cardId=String(body?.cardId||"").trim();
            const requestId=String(body?.requestId||"").trim();
            if(!cardId)return;
            try{
              const learning=await loadLearningData();
              const card=learning.cards.find(item=>String(item.id)===cardId);
              if(!card)return;
              const activeRequest=String(card.imageGeneration?.requestId||"");
              if(requestId&&activeRequest&&activeRequest!==requestId)return;
              const now=new Date().toISOString();
              if(errorInfo){
                card.visualImagePendingAtAdvance=false;
                card.imageGeneration={status:"error",phase:"error",message:String(errorInfo.message||"这次图片没有生成成功，可以稍后重试。"),code:String(errorInfo.code||"IMAGE_GENERATION_FAILED"),requestId,finishedAt:now};
              }else if(image?.url){
                const stage=String(card.learningStage||card.stage||"").toLowerCase();
                const autoConfirm=stage!=="visualize"&&card.visualImagePendingAtAdvance===true;
                card.imageData="";
                card.imageUrl=String(image.url);
                card.generatedVisualScene=String(image.visualNote||body.visualNote||body.suggestedScene||"").trim();
                card.visualImageSource="generated";
                card.visualImagePreparedAt=now;
                card.visualImageConfirmed=autoConfirm?true:false;
                if(autoConfirm)card.visualImageConfirmedAt=now;
                card.visualImagePendingAtAdvance=false;
                card.imageGeneration={status:"success",phase:"done",message:autoConfirm?"联想图已在后台生成并保存到单词卡。":"联想图已生成并保存为 Visualize 草稿。",code:"",requestId,finishedAt:now};
              }
              card.updatedAt=now;
              await saveLearningData(learning);
            }catch(err){
              console.warn("background visual attachment skipped:",err?.message||err);
            }
          }
'''
          t=replace_once(t,'\nasync function generateVisual(body) {',helper+'\nasync function generateVisual(body) {','server background helper')
          old_route='''    if (req.method === "POST" && url.pathname === "/api/ai/image") {
      const body = await readJsonBody(req);
      try {
        const image = await generateVisual(body);
        return sendJson(res, 200, { ok: true, image });
      } catch (err) {
        console.error("image AI failed:", err?.message || err);
        const friendly = friendlyError(err, "image");
        return sendJson(res, 502, { ok: false, code: friendly.code, error: friendly.message, userError: friendly });
      }
    }'''
          new_route='''    if (req.method === "POST" && url.pathname === "/api/ai/image") {
      const body = await readJsonBody(req);
      try {
        const image = await generateVisual(body);
        await attachVisualGenerationToCard(body,image);
        return sendJson(res, 200, { ok: true, image });
      } catch (err) {
        console.error("image AI failed:", err?.message || err);
        const friendly = friendlyError(err, "image");
        await attachVisualGenerationToCard(body,null,{code:friendly.code,message:friendly.message});
        return sendJson(res, 502, { ok: false, code: friendly.code, error: friendly.message, userError: friendly });
      }
    }'''
          t=replace_once(t,old_route,new_route,'server image route ownership')
          write(p,t)

          # 7) Regression contract.
          check=Path('scripts/check-visualize-product-flow-v3.js')
          check.write_text('''const fs=require("fs");\nconst path=require("path");\nfunction read(p){return fs.readFileSync(path.join(__dirname,"..",p),"utf8").replace(/\\r\\n/g,"\\n");}\nfunction assert(v,m){if(!v)throw new Error(m);}\nconst ai=read("public/ai-assist-v3.js");\nconst visual=read("public/visualize-stage-v3.js");\nconst guard=read("public/visualize-input-guard-v3.js");\nconst actions=read("public/visualize-actions-v3.js");\nconst transition=read("public/stage-transition-v3.js");\nconst server=read("server.js");\nassert(ai.includes("userScene")&&!ai.includes("sceneAvoidance("),"AI concrete-scene flow must preserve the user's scene instead of avoiding it");\nassert(server.includes("用户原始联想（最高优先级，必须保留）"),"Server prompt must anchor on the user's own scene");\nassert(guard.includes("LexiFlowVisualizeInputGuardV3")&&guard.includes("clearDraft"),"Visual input needs one shared draft authority");\nassert(visual.includes("图片后台生成中 · 完成视觉联想")&&visual.includes("saveCardAnyStagePatch"),"Image generation must not block stage completion");\nassert(visual.includes("cardId:card.id,requestId"),"Background image job must identify its card and request");\nassert(actions.includes('pending=current.imageGeneration?.status==="generating"'),"Visual action decorator must treat a pending image as continuable");\nassert(transition.includes("visualImagePendingAtAdvance"),"Stage transition must preserve pending generation intent");\nassert(server.includes("attachVisualGenerationToCard")&&server.includes("await attachVisualGenerationToCard(body,image)"),"Server must attach background image results even after navigation");\nassert(visual.includes("lexi-v3-generation-dots")&&!visual.includes('<span class="mini-spinner"></span><strong>正在生成联想图'),"Visual generation state must use the refined non-blocking animation");\nconsole.log("Visualize product-flow checks passed.");\n''',encoding='utf-8')

          p='package.json'; pkg=json.loads(read(p)); check_cmd='node scripts/check-visualize-product-flow-v3.js'
          if check_cmd not in pkg['scripts']['check:learning']:
              pkg['scripts']['check:learning'] += ' && ' + check_cmd
          if 'node --check scripts/check-visualize-product-flow-v3.js' not in pkg['scripts']['check']:
              pkg['scripts']['check']=pkg['scripts']['check'].replace(' && node --check public/example-hydration.js',' && node --check scripts/check-visualize-product-flow-v3.js && node --check public/example-hydration.js')
          write(p,json.dumps(pkg,ensure_ascii=False,indent=2)+'\n')
          PY

      - name: Validate Visualize fix
        run: |
          node --check public/ai-assist-v3.js
          node --check public/visualize-input-guard-v3.js
          node --check public/visualize-stage-v3.js
          node --check public/visualize-actions-v3.js
          node --check public/stage-transition-v3.js
          node --check server.js
          node scripts/check-visualize-product-flow-v3.js
          npm run check:learning

      - name: Commit product fix and remove one-shot workflow
        shell: bash
        run: |
          rm .github/workflows/one-shot-visualize-product-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/ai-assist-v3.js public/visualize-input-guard-v3.js public/visualize-stage-v3.js public/visualize-actions-v3.js public/stage-transition-v3.js server.js scripts/check-visualize-product-flow-v3.js package.json .github/workflows/one-shot-visualize-product-fix.yml
          git commit -m "fix: align Visualize with user scene and background generation"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
